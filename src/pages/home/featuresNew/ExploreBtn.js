import { Button } from "@mui/material";
import React from "react";

const ExploreBtn = () => {
  return (
    <Button
      sx={{
        marginTop: "20px",
      }}
      variant="contained"
    >
      Explore more
    </Button>
  );
};

export default ExploreBtn;
